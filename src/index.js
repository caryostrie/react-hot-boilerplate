import React from 'react';
import ReactDOM from 'react-dom';

import SearchControl from './SearchControl';

ReactDOM.render(<SearchControl placeholder={'Search for shortcuts...'} />, document.getElementById('container'));
